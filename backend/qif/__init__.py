"""Generic multi-XSD engineering-standard ingestion service.

The package is intentionally independent from the general import pipeline so
the schema-set API can evolve as a separately deployable microservice.
"""
