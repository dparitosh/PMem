# Minimal runtime for the independently deployable DEPO services.
# Keep optional LLM, OCR, notebooks, and legacy-main dependencies in
# requirements.txt; they do not belong in the microservice image.
fastapi>=0.116,<1
uvicorn[standard]>=0.35,<1
pydantic>=2.11,<3
python-multipart>=0.0.20
httpx>=0.28,<0.29
defusedxml>=0.7,<1
neo4j>=5.28,<6
rdflib>=7.6,<8
semantica==0.6.7
openpyxl>=3.1,<4
